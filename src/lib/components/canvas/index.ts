import Canvas from './canvas.svelte';
import { defaultBackgroundOptions, defaultForegroundOptions } from './canvas-builder';
import type { BackgroundOptions, ForegroundOptions } from './canvas-types';

export {
	Canvas,
	defaultBackgroundOptions,
	defaultForegroundOptions,
	type BackgroundOptions,
	type ForegroundOptions
};
